name: researcher
description:
  Example demonstrating how to use the library merging_builder.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:

dev_dependencies:
  build_runner: ^1.10.0
  build_test: ^1.2.0
  researcher_builder:
    path: ../researcher_builder
  source_gen: ^0.9.6
  test: ^1.15.2
