name: researcher_builder
description:
  Example demonstrating how to use the library merging_builder.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  build: ^1.3.0
  #source_gen: ^0.9.5
  merging_builder:
    path: ../../

dev_dependencies:
