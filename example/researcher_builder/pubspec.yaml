name: researcher_builder
description:
  Example demonstrating how to use the library merging_builder.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  analyzer: ^0.39.12
  build: ^1.3.0
  build_config: ^0.4.2
  merging_builder:
    path: ../../
  researcher:
    path: ../researcher
  source_gen: ^0.9.5
  quote_buffer: ^0.1.5

dev_dependencies:
